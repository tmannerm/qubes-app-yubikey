# Enter here SHA1 hash of the password used for login when YubiKey is present.
# Optionally might be left empty.

# To generate SHA1 hash, you can use this command (replace "PASSWORD" with
# actual password):
#
#    echo -n "PASSWORD" | openssl dgst -sha1
#
# If you fill this value, the plaintext version in `yk-login-pass` file will be
# ignored.

# changeme
